[package]
name = "pg-gen"
version = "0.1.0"
edition = "2024"
description = "Library for generating Kubernetes ConfigMaps from SQL files"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
eyre = "0.6"
